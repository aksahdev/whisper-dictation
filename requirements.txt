# Python packages for Windows
openai
sounddevice
soundfile
pynput
numpy
groq
pyautogui
python-dotenv
pyperclip
pywin32

# Additional Windows notes:
# - pyautogui is the primary text input method (recommended)
# - pynput provides keyboard input and is always available
# - pywin32 provides clipboard fallback (optional but recommended)
# - pyperclip provides cross-platform clipboard (alternative to pywin32)
