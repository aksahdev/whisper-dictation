# Python packages
openai
sounddevice
soundfile
pynput
numpy
groq
pyautogui
python-dotenv

# System packages (install with apt/package manager):
# For Wayland (Hyprland, GNOME Wayland, etc.):
#   sudo apt install wtype ydotool ydotoold
# For X11 (traditional desktops):
#   sudo apt install xdotool
# 
# The script will automatically detect your environment and use the appropriate tool.
